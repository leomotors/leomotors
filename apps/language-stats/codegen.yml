overwrite: true
schema:
  - "https://api.github.com/graphql":
      headers:
        Authorization: "Bearer ${GH_PAT}"

documents: "src/loadData.ts"
generates:
  generated/graphql.ts:
    - "typescript"
    - "typescript-operations"
